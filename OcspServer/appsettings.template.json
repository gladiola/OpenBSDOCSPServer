{
  "_Notice": "Copy this file to appsettings.json and fill in real values before running.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "FeatureFlags": {
    "EnableSession": true,
    "EnableSecurityHeaders": true,
    "EnableMtls": false,
    "EnableAdminAuth": true,
    "EnableOcspNonce": true
  },

  "OcspServer": {
    "_Comment_ResponderCertPath": "PEM certificate for the OCSP signing identity issued by the offline CA",
    "ResponderCertPath": "/etc/ocsp/responder.crt",

    "_Comment_SigningKeyPath": "PEM private key for the OCSP signing identity",
    "SigningKeyPath": "/etc/ocsp/responder.key",

    "_Comment_SigningKeyPassword": "Passphrase for encrypted key, or null/omit for unencrypted",
    "SigningKeyPassword": null,

    "_Comment_NextUpdateHours": "Clients must not cache responses past thisUpdate + NextUpdateHours",
    "NextUpdateHours": 24,

    "AllowNonce": true,
    "RequireNonce": false,
    "MaxNonceSizeBytes": 32,
    "AllowGetRequests": true,
    "AllowPostRequests": true
  },

  "AdminAuth": {
    "AdminUsername": "admin",

    "_Comment_AdminPasswordHash": "Generate with: dotnet run -- hash-password  Format: iterations:base64salt:base64hash",
    "AdminPasswordHash": "310000:BASE64SALT==:BASE64HASH==",

    "SessionTimeoutMinutes": 30
  },

  "Ingestion": {
    "_Comment_IndexTxtWatchPath": "Path to OpenSSL CA index.txt. Leave null to disable file-watch auto-import.",
    "IndexTxtWatchPath": null,

    "_Comment_PollingIntervalMinutes": "0 = manual import only",
    "PollingIntervalMinutes": 0,

    "_Comment_LocalOcspResponderUrl": "Base URL of a local OpenSSL OCSP responder for live delta sync, e.g. http://127.0.0.1:2560",
    "LocalOcspResponderUrl": null,

    "DatabasePath": "ocsp.db"
  }
}
